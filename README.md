# Run VibeJS Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Create a `.env.local` file at the project root and set `VITE_GEMINI_API_KEY` to your Gemini API key. Example is provided in `.env.local.example`.
3. Run the app:
   `npm run dev`
